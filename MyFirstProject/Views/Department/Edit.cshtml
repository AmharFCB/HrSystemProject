@model Department
@{
    ViewData["Title"] = "Edit Department";
}

<div class="container mt-4">
    <div class="card shadow-sm border-warning">
        <div class="card-header bg-warning text-white">
            <h5 class="mb-0"><i class="fas fa-building me-2"></i> Edit Department</h5>
        </div>
        <div class="card-body">
            <form method="post" asp-action="Edit">
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="Id" />
                <input type="hidden" asp-for="Uid" />
                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label for="DepartmentName" class="form-label">Department Name</label>
                        <input type="text" asp-for="DepartmentName" class="form-control" placeholder="Enter department name">
                        <span asp-validation-for="DepartmentName" class="text-danger"></span>
                    </div>

                    <div class="col-md-6 mb-3">
                        <label for="EmployeesId" class="form-label">Department Manager</label>
                        <select asp-for="EmployeesId" class="form-select" asp-items="@ViewBag.EmployeesList">
                            <option value="" disabled selected>Select Manager</option>
                        </select>
                        <span asp-validation-for="EmployeesId" class="text-danger"></span>
                    </div>
                </div>

                <div class="d-flex justify-content-between mt-3">
                    <a asp-action="Index" class="btn btn-outline-secondary">
                        <i class="fas fa-arrow-left"></i> Back
                    </a>
                    <button type="submit" class="btn btn-warning text-white">
                        <i class="fas fa-save"></i> Update
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
